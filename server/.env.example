# Server Configuration
PORT=3000

# AI Provider Configuration
# Choose 'openai' (fast, paid) or 'ollama' (free, slow)
AI_PROVIDER=openai

# OpenAI Configuration (Recommended for development - 10-50x faster than Ollama)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here
AI_MODEL=gpt-4o-mini

# Cost estimates for OpenAI:
# - gpt-4o-mini: ~$0.15 per 1M input tokens, ~$0.60 per 1M output tokens
# - Fixing 123 questions: ~$0.05-0.10 total
# - Validating all Math papers: ~$0.02-0.05

# Ollama Configuration (Free but slow - good for offline/no-cost development)
# OLLAMA_BASE_URL=http://localhost:11434
# AI_MODEL=mistral
# Available models: llama3.2, phi3, gemma2, qwen2.5, mistral

# Alternative: Google Gemini (Has free tier)
# AI_PROVIDER=gemini
# GEMINI_API_KEY=your-key-here
# AI_MODEL=gemini-1.5-flash
